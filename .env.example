# Base URL for Parabank
# Include context path `/parabank` to ensure correct routing
BASE_URL=https://parabank.parasoft.com/parabank

# Demo account credentials (used for UI login/auth.json)
PARABANK_USER=john
PARABANK_PASSWORD=demo

# Customer ID for API tests (default demo user is 12212)
CUSTOMER_ID=12212

# Force regeneration of auth.json
# Set to 1 to refresh login state on next run, leave 0 to reuse
FORCE_AUTH=0

